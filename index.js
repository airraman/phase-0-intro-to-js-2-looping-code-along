function writeCards(names, eventName){

    

for( let i = 0; i < names.length; i++){
    return `Thank you ${names[i]} for the wonderful ${eventName} gift`
}
}
eventName = "wedding"

writeCards(names = ["Raman","Dylan", "Tor"], eventName)